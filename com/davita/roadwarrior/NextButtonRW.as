package com.davita.roadwarrior{	import flash.display.*;	import flash.events.*;	/**	 * ...	 * @author Ian Kennedy	 */	public class NextButtonRW extends MovieClip	{		//---------------------------------------		// PRIVATE VARIABLES		//---------------------------------------		private var _frameLabel:String;		//---------------------------------------		// CONSTRUCTOR		//---------------------------------------				// on instantiation, the NextButtonRW can be passed a frameLabel		public function NextButtonRW(frameLabel:String = ""):void		{			this._frameLabel = frameLabel;			if (stage)			{				init();			}			else			{				addEventListener(Event.ADDED_TO_STAGE,init);			}		}		//---------------------------------------		// PRIVATE METHODS		//---------------------------------------		private function init(e:Event=null):void		{			trace("NextButtonRW::init()");			removeEventListener(Event.ADDED_TO_STAGE,init);			this.addEventListener(MouseEvent.CLICK, nextButtonClickHandler);						// set up NextButtonRW properties			this.buttonMode = true;			this.useHandCursor = true;		}				private function nextButtonClickHandler(event:MouseEvent):void		{			this.parent.gotoAndStop(_frameLabel);		}			}}